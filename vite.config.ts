import { defineConfig } from 'vite';

/**
 * Configuración de compilación y desarrollo de VME.
 * Se mantiene deliberadamente libre de plugins para preservar la superficie inicial de dependencias.
 */
export default defineConfig({});
